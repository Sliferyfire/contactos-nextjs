generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Contactos {
  id         BigInt   @id(map: "CONTACTOS_pkey") @default(autoincrement())
  created_at DateTime @default(now()) @db.Timestamptz(6)
  nombre     String?
  correo     String?
  telefono   String?
  estado     String?
}
